define(function (require) {
  // FIXME: angular has its own rules to load modules
  //        paths in seajs.config is helpless
  var prefix = {
    api: '/yingyuan/scripts/apimock/',
    partials: '/yingyuan/scripts/partials'
  };

  return prefix;
});